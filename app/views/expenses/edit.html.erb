<h1>Editing expense</h1>

<%= render "form", expense: @expense %>

<br>

<div>
  <%= link_to "Show this expense", group_expense_path(@group, @expense) %> |
  <%= link_to "Back to expenses", group_expenses_path(@group) %>
</div>
